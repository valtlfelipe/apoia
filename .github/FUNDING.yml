# Powers the "Sponsor" button on the repository page. Two ways to give: the
# custom link is apoia's own apoia page — the same one behind "Apoiar o
# projeto" in /admin/about (PROJECT.supportUrl in lib/project.ts).
github: valtlfelipe
custom: ["https://apoia.felipevm.com/apoia"]
